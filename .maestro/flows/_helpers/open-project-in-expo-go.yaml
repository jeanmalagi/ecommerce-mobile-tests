# Abre o projeto no Expo Go quando estiver na tela Home do Expo
appId: host.exp.exponent
---

- openLink: ${EXPO_URL}

- runFlow:
    when:
      visible: "Enter URL manually"
    file: ./open-project-via-url.yaml

- extendedWaitUntil:
    visible:
      id: "input-email"
    timeout: 60000
